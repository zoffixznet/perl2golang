my @nums = (3, 1, 4, 1, 5);
print scalar(@nums), "\n";
my %seen;
$seen{$_}++ for @nums;
sub trim {
    my $s = shift;
    $s =~ s/^\s+|\s+$//g;
    return $s;
}
my $broken = ;
eval "1 + 1";
:vars
:concepts
:go full
:quit
